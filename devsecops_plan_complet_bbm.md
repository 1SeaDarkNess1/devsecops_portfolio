# Plan Complet Proiect DevSecOps Personal## Bogdan Mihai Bontoș · Vară 2026\---## De ce acest proiect și nu altcevaÎnainte să intri în detalii tehnice, înțelege **de ce** merită:Un recrutor de la o companie tech îți vede CV-ul. Scrie "DevSecOps" ca obiectiv. Are în față 50 de candidați care spun același lucru. Tu ai un link GitHub cu un proiect real, un dashboard Grafana live, un pipeline CI/CD funcțional și un raport de securitate generat automat. Ceilalți 49 au "am studiat DevSecOps".Asta e diferența pe care o face acest proiect.\---## Ce vei construi — imaginea de ansambluO aplicație web simplă (portfolio personal sau un tool util) înconjurată de **toată infrastructura pe care o folosesc companiile reale**:```Developer (tu, local)    │    │  git push    ▼GitHub Repository    │    │  trigger automat    ▼GitHub Actions CI/CD Pipeline    ├── Lint \\\\\\\& Test (verifică codul)    ├── SAST — Semgrep (analiză statică securitate)    ├── Build Docker Image    ├── Trivy Scan (vulnerabilități în imagine)    ├── Push to Container Registry    └── Deploy automat pe VPS            │            ▼        VPS Oracle Cloud            ├── Nginx (reverse proxy + SSL)            ├── Aplicația ta (Docker container)            ├── Prometheus (colectare metrici)            ├── Grafana (dashboard vizual)            ├── Loki (agregare loguri)            ├── Wazuh (IDS/SIEM)            └── Fail2ban + UFW (protecție)```\---## Stack tehnologic complet|Categorie|Tehnologie|De ce||-|-|-||Aplicație|Python Flask sau Node.js|Simplu, documentat, recunoscut||Container|Docker + Compose|Standard industrie||CI/CD|GitHub Actions|Gratuit, integrat cu GitHub||SAST|Semgrep|Cel mai bun open-source SAST||Container scan|Trivy|Standard pentru vulnerabilități Docker||Secret scan|GitLeaks|Detectare chei/parole în cod||Web server|Nginx|Standard industrie||SSL|Let's Encrypt + Certbot|Gratuit, automat||Metrici|Prometheus + Node Exporter|Standard industrie||Dashboard|Grafana|Vizualizare profesională||Loguri|Loki + Promtail|Stack Grafana complet||SIEM/IDS|Wazuh|Enterprise-grade, open-source||Firewall|UFW + Fail2ban|Protecție de bază solidă||VPS|Oracle Cloud Free Tier|Gratuit permanent||DNS|CloudFlare|Gratuit + protecție DDoS|\---## Faza 0 — Aplicația (Săptămâna 1-2)### Ce aplicație faciNu complica — scopul nu e aplicația, ci infrastructura din jur. Alege una:**Opțiunea A — Portfolio personal** (recomandat)* Pagină web cu proiectele tale, CV, contact* Backend simplu în Flask sau Node.js* Arată profesional în interviuri**Opțiunea B — URL Shortener*** Introduci un URL lung, primești unul scurt* Funcționalitate clară, ușor de testat* Demonstrează că înțelegi backend-ul**Opțiunea C — System Monitor Dashboard*** Afișează metrici despre server în timp real* Direct relevant pentru DevSecOps* Impresionează tehnic### Structura aplicației (exemplu Flask)```myapp/├── app/│   ├── \\\\\\\_\\\\\\\_init\\\\\\\_\\\\\\\_.py│   ├── routes.py│   ├── models.py│   └── templates/│       └── index.html├── tests/│   ├── test\\\\\\\_routes.py│   └── test\\\\\\\_models.py├── Dockerfile├── docker-compose.yml├── requirements.txt├── .env.example└── README.md```### Dockerfile aplicație```dockerfile# Multi-stage build — imagine mai mică și mai sigurăFROM python:3.11-slim AS builderWORKDIR /appCOPY requirements.txt .RUN pip install --no-cache-dir --user -r requirements.txtFROM python:3.11-slim AS runner# User non-root — securitateRUN useradd -m -u 1001 appuserWORKDIR /appCOPY --from=builder /root/.local /home/appuser/.localCOPY --chown=appuser:appuser app/ ./app/USER appuser# Nu rula ca root niciodatăEXPOSE 5000HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \\\\\\\\    CMD curl -f http://localhost:5000/health || exit 1CMD \\\\\\\["python", "-m", "flask", "run", "--host=0.0.0.0"]```### docker-compose.yml aplicație```yamlversion: '3.8'services:  app:    build: .    container\\\\\\\_name: myapp    restart: unless-stopped    ports:      - "127.0.0.1:5000:5000"    environment:      - FLASK\\\\\\\_ENV=production      - SECRET\\\\\\\_KEY=${SECRET\\\\\\\_KEY}    env\\\\\\\_file:      - .env    networks:      - app\\\\\\\_net    labels:      - "logging=promtail"    # pentru Loki  # Baza de date (dacă e nevoie)  db:    image: postgres:15-alpine    container\\\\\\\_name: myapp-db    restart: unless-stopped    environment:      POSTGRES\\\\\\\_DB: ${DB\\\\\\\_NAME}      POSTGRES\\\\\\\_USER: ${DB\\\\\\\_USER}      POSTGRES\\\\\\\_PASSWORD: ${DB\\\\\\\_PASS}    volumes:      - db\\\\\\\_data:/var/lib/postgresql/data    networks:      - app\\\\\\\_netvolumes:  db\\\\\\\_data:networks:  app\\\\\\\_net:    driver: bridge```\---## Faza 1 — VPS și securitate de bază (Săptămâna 2-3)### Setup inițial securizat```bash# Oracle Cloud Free Tier — Ubuntu 22.04 LTS# La creare: uploadezi cheia SSH publică# Prima conectaressh ubuntu@IP\\\\\\\_VPS# Update completsudo apt update \\\\\\\&\\\\\\\& sudo apt upgrade -ysudo apt install -y curl wget git ufw fail2ban nginx certbot \\\\\\\\    python3-certbot-nginx docker.io docker-compose-plugin \\\\\\\\    htop tmux net-tools# User personalsudo useradd -m -s /bin/bash -G sudo,docker bogdansudo passwd bogdan# Copiază cheile SSHsudo mkdir -p /home/bogdan/.sshsudo cp \\\\\\\~/.ssh/authorized\\\\\\\_keys /home/bogdan/.ssh/sudo chown -R bogdan:bogdan /home/bogdan/.sshsudo chmod 700 /home/bogdan/.sshsudo chmod 600 /home/bogdan/.ssh/authorized\\\\\\\_keys```### SSH Hardening complet```bashsudo nano /etc/ssh/sshd\\\\\\\_config``````Port 2222PermitRootLogin noPasswordAuthentication noPubkeyAuthentication yesMaxAuthTries 3X11Forwarding noAllowUsers bogdanClientAliveInterval 300ClientAliveCountMax 2LogLevel VERBOSE``````bashsudo sshd -t \\\\\\\&\\\\\\\& sudo systemctl restart sshd```### UFW Firewall```bashsudo ufw default deny incomingsudo ufw default allow outgoingsudo ufw allow 2222/tcp comment 'SSH'sudo ufw allow 80/tcp comment 'HTTP'sudo ufw allow 443/tcp comment 'HTTPS'sudo ufw limit 2222/tcpsudo ufw enable```### Fail2ban```bashsudo nano /etc/fail2ban/jail.local``````ini\\\\\\\[DEFAULT]bantime = 86400findtime = 600maxretry = 3\\\\\\\[sshd]enabled = trueport = 2222logpath = /var/log/auth.logmaxretry = 3``````bashsudo systemctl enable fail2ban \\\\\\\&\\\\\\\& sudo systemctl start fail2ban```\---## Faza 2 — CI/CD Pipeline cu GitHub Actions (Săptămâna 3-4)### De ce GitHub Actions* Gratuit pentru repository-uri publice* Integrat direct în GitHub* Standard în industrie* Rulează automat la fiecare `git push`### Structura pipeline-ului```.github/└── workflows/    ├── ci.yml          ← teste + security scan    └── deploy.yml      ← deploy pe VPS```### ci.yml — Pipeline complet```yamlname: CI Security Pipelineon:  push:    branches: \\\\\\\[ main, develop ]  pull\\\\\\\_request:    branches: \\\\\\\[ main ]jobs:  # ── JOB 1: Teste ──  test:    name: Run Tests    runs-on: ubuntu-latest        steps:      - name: Checkout cod        uses: actions/checkout@v4            - name: Setup Python        uses: actions/setup-python@v4        with:          python-version: '3.11'            - name: Instalare dependențe        run: |          pip install -r requirements.txt          pip install pytest pytest-cov            - name: Rulare teste        run: pytest tests/ --cov=app --cov-report=xml            - name: Upload coverage        uses: codecov/codecov-action@v3  # ── JOB 2: SAST cu Semgrep ──  sast:    name: Security Analysis (SAST)    runs-on: ubuntu-latest    needs: test        steps:      - uses: actions/checkout@v4            - name: Semgrep SAST Scan        uses: semgrep/semgrep-action@v1        with:          config: >-            p/python            p/owasp-top-ten            p/secrets        env:          SEMGREP\\\\\\\_APP\\\\\\\_TOKEN: ${{ secrets.SEMGREP\\\\\\\_APP\\\\\\\_TOKEN }}    # ── JOB 3: Secret Scanning ──  secrets-scan:    name: Secret Scanning    runs-on: ubuntu-latest        steps:      - uses: actions/checkout@v4        with:          fetch-depth: 0            - name: GitLeaks - detectare secrete        uses: gitleaks/gitleaks-action@v2        env:          GITHUB\\\\\\\_TOKEN: ${{ secrets.GITHUB\\\\\\\_TOKEN }}  # ── JOB 4: Build și Trivy Scan ──  build-and-scan:    name: Build Docker + Vulnerability Scan    runs-on: ubuntu-latest    needs: \\\\\\\[test, sast]        steps:      - uses: actions/checkout@v4            - name: Build Docker image        run: docker build -t myapp:${{ github.sha }} .            - name: Trivy - scan vulnerabilități        uses: aquasecurity/trivy-action@master        with:          image-ref: myapp:${{ github.sha }}          format: 'sarif'          output: 'trivy-results.sarif'          severity: 'CRITICAL,HIGH'          exit-code: '1'    # eșuează dacă găsește CRITICAL            - name: Upload rezultate Trivy        uses: github/codeql-action/upload-sarif@v2        if: always()        with:          sarif\\\\\\\_file: 'trivy-results.sarif'            - name: Push imagine în registry        if: github.ref == 'refs/heads/main'        run: |          echo ${{ secrets.GHCR\\\\\\\_TOKEN }} | \\\\\\\\            docker login ghcr.io -u ${{ github.actor }} --password-stdin          docker tag myapp:${{ github.sha }} \\\\\\\\            ghcr.io/${{ github.repository }}:${{ github.sha }}          docker tag myapp:${{ github.sha }} \\\\\\\\            ghcr.io/${{ github.repository }}:latest          docker push ghcr.io/${{ github.repository }}:${{ github.sha }}          docker push ghcr.io/${{ github.repository }}:latest```### deploy.yml — Deploy automat pe VPS```yamlname: Deploy to VPSon:  workflow\\\\\\\_run:    workflows: \\\\\\\["CI Security Pipeline"]    types:      - completed    branches: \\\\\\\[ main ]jobs:  deploy:    name: Deploy    runs-on: ubuntu-latest    if: ${{ github.event.workflow\\\\\\\_run.conclusion == 'success' }}        steps:      - name: Deploy pe VPS via SSH        uses: appleboy/ssh-action@v1.0.0        with:          host: ${{ secrets.VPS\\\\\\\_HOST }}          username: bogdan          key: ${{ secrets.VPS\\\\\\\_SSH\\\\\\\_KEY }}          port: 2222          script: |            cd \\\\\\\~/myapp                        # Pull ultima imagine            echo ${{ secrets.GHCR\\\\\\\_TOKEN }} | \\\\\\\\              docker login ghcr.io -u ${{ github.actor }} --password-stdin            docker compose pull                        # Deploy cu zero downtime            docker compose up -d --remove-orphans                        # Verificare health            sleep 10            curl -f http://localhost:5000/health || \\\\\\\\              (docker compose logs app \\\\\\\&\\\\\\\& exit 1)                        # Curățare imagini vechi            docker image prune -f                        echo "Deploy reușit la $(date)"```\---## Faza 3 — Monitoring cu Prometheus și Grafana (Săptămâna 4-5)### Arhitectura monitoring```Aplicație → Prometheus → Grafana    │              ↑    └── metrici    │                   │Node Exporter ─────┘ (metrici sistem: CPU, RAM, disk)cAdvisor ──────────┘ (metrici Docker containere)```### docker-compose.monitoring.yml```yamlversion: '3.8'services:  # Prometheus — colectare metrici  prometheus:    image: prom/prometheus:latest    container\\\\\\\_name: prometheus    restart: unless-stopped    ports:      - "127.0.0.1:9090:9090"    volumes:      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml      - prometheus\\\\\\\_data:/prometheus    command:      - '--config.file=/etc/prometheus/prometheus.yml'      - '--storage.tsdb.retention.time=30d'    networks:      - monitoring\\\\\\\_net  # Grafana — dashboard vizual  grafana:    image: grafana/grafana:latest    container\\\\\\\_name: grafana    restart: unless-stopped    ports:      - "127.0.0.1:3000:3000"    environment:      - GF\\\\\\\_SECURITY\\\\\\\_ADMIN\\\\\\\_PASSWORD=${GRAFANA\\\\\\\_PASS}      - GF\\\\\\\_USERS\\\\\\\_ALLOW\\\\\\\_SIGN\\\\\\\_UP=false      - GF\\\\\\\_SERVER\\\\\\\_DOMAIN=${DOMAIN}    volumes:      - grafana\\\\\\\_data:/var/lib/grafana      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources    networks:      - monitoring\\\\\\\_net  # Node Exporter — metrici sistem  node-exporter:    image: prom/node-exporter:latest    container\\\\\\\_name: node-exporter    restart: unless-stopped    volumes:      - /proc:/host/proc:ro      - /sys:/host/sys:ro      - /:/rootfs:ro    command:      - '--path.procfs=/host/proc'      - '--path.sysfs=/host/sys'    networks:      - monitoring\\\\\\\_net  # cAdvisor — metrici Docker  cadvisor:    image: gcr.io/cadvisor/cadvisor:latest    container\\\\\\\_name: cadvisor    restart: unless-stopped    volumes:      - /:/rootfs:ro      - /var/run:/var/run:ro      - /sys:/sys:ro      - /var/lib/docker/:/var/lib/docker:ro    networks:      - monitoring\\\\\\\_net  # Loki — agregare loguri  loki:    image: grafana/loki:latest    container\\\\\\\_name: loki    restart: unless-stopped    ports:      - "127.0.0.1:3100:3100"    volumes:      - loki\\\\\\\_data:/loki      - ./monitoring/loki.yml:/etc/loki/config.yml    networks:      - monitoring\\\\\\\_net  # Promtail — colectare loguri  promtail:    image: grafana/promtail:latest    container\\\\\\\_name: promtail    restart: unless-stopped    volumes:      - /var/log:/var/log:ro      - /var/lib/docker/containers:/var/lib/docker/containers:ro      - ./monitoring/promtail.yml:/etc/promtail/config.yml    networks:      - monitoring\\\\\\\_netvolumes:  prometheus\\\\\\\_data:  grafana\\\\\\\_data:  loki\\\\\\\_data:networks:  monitoring\\\\\\\_net:    driver: bridge```### monitoring/prometheus.yml```yamlglobal:  scrape\\\\\\\_interval: 15s  evaluation\\\\\\\_interval: 15sscrape\\\\\\\_configs:  - job\\\\\\\_name: 'prometheus'    static\\\\\\\_configs:      - targets: \\\\\\\['localhost:9090']  - job\\\\\\\_name: 'node'    static\\\\\\\_configs:      - targets: \\\\\\\['node-exporter:9100']  - job\\\\\\\_name: 'cadvisor'    static\\\\\\\_configs:      - targets: \\\\\\\['cadvisor:8080']  - job\\\\\\\_name: 'myapp'    static\\\\\\\_configs:      - targets: \\\\\\\['myapp:5000']    metrics\\\\\\\_path: '/metrics'```### Dashboard-uri Grafana recomandateImportă din grafana.com (ID-uri):* **1860** — Node Exporter Full (metrici sistem)* **193** — Docker and system monitoring* **13639** — Loki Logs Dashboard\---## Faza 4 — SIEM cu Wazuh (Săptămâna 5-6)### Ce e Wazuh și de ce conteazăWazuh = platformă open-source de securitate folosită în companii reale (echivalent commercial: Splunk, IBM QRadar). Detectează:* Intruziuni și atacuri* Modificări neautorizate ale fișierelor (FIM)* Vulnerabilități în sistem* Conformitate cu standarde (PCI DSS, GDPR)Pe CV, "am configurat și administrat Wazuh" = experiență relevantă direct pentru SecOps.### Instalare Wazuh (all-in-one pe VPS)```bash# Necesită minim 4GB RAM — Oracle Free Tier are 24GB, ești okcurl -sO https://packages.wazuh.com/4.7/wazuh-install.shsudo bash wazuh-install.sh -a# Accesezi dashboard-ul la https://IP\\\\\\\_VPS# Credențiale afișate la final de instalare```### Ce configurezi în Wazuh**File Integrity Monitoring (FIM):**```xml<!-- /var/ossec/etc/ossec.conf --><syscheck>  <frequency>300</frequency>  <directories check\\\\\\\_all="yes">/etc,/usr/bin,/usr/sbin</directories>  <directories check\\\\\\\_all="yes" report\\\\\\\_changes="yes">/home/bogdan</directories>  <ignore>/etc/mtab</ignore></syscheck>```**Alerte pentru atacuri SSH:**Wazuh detectează automat tentativele de brute force și le corelează cu Fail2ban.**Active Response — blocare automată IP-uri:**```xml<active-response>  <command>firewall-drop</command>  <location>local</location>  <rules\\\\\\\_id>5763</rules\\\\\\\_id>  <timeout>600</timeout></active-response>```\---## Faza 5 — Nginx și SSL (Săptămâna 6)### Configurare Nginx completă```bashsudo nano /etc/nginx/sites-available/myapp``````nginx# Redirect HTTP → HTTPSserver {    listen 80;    server\\\\\\\_name domeniu-tau.ro www.domeniu-tau.ro;    return 301 https://$server\\\\\\\_name$request\\\\\\\_uri;}# Aplicația principalăserver {    listen 443 ssl http2;    server\\\\\\\_name domeniu-tau.ro www.domeniu-tau.ro;    ssl\\\\\\\_certificate /etc/letsencrypt/live/domeniu-tau.ro/fullchain.pem;    ssl\\\\\\\_certificate\\\\\\\_key /etc/letsencrypt/live/domeniu-tau.ro/privkey.pem;    ssl\\\\\\\_protocols TLSv1.2 TLSv1.3;    ssl\\\\\\\_prefer\\\\\\\_server\\\\\\\_ciphers off;        # Security headers    add\\\\\\\_header Strict-Transport-Security "max-age=63072000" always;    add\\\\\\\_header X-Frame-Options DENY;    add\\\\\\\_header X-Content-Type-Options nosniff;    add\\\\\\\_header X-XSS-Protection "1; mode=block";    add\\\\\\\_header Content-Security-Policy "default-src 'self'";    add\\\\\\\_header Referrer-Policy "strict-origin-when-cross-origin";    # Rate limiting    limit\\\\\\\_req\\\\\\\_zone $binary\\\\\\\_remote\\\\\\\_addr zone=app:10m rate=10r/s;    limit\\\\\\\_req zone=app burst=20 nodelay;    location / {        proxy\\\\\\\_pass http://127.0.0.1:5000;        proxy\\\\\\\_set\\\\\\\_header Host $host;        proxy\\\\\\\_set\\\\\\\_header X-Real-IP $remote\\\\\\\_addr;        proxy\\\\\\\_set\\\\\\\_header X-Forwarded-For $proxy\\\\\\\_add\\\\\\\_x\\\\\\\_forwarded\\\\\\\_for;        proxy\\\\\\\_set\\\\\\\_header X-Forwarded-Proto $scheme;    }}# Grafana — subdomainserver {    listen 443 ssl http2;    server\\\\\\\_name grafana.domeniu-tau.ro;    ssl\\\\\\\_certificate /etc/letsencrypt/live/domeniu-tau.ro/fullchain.pem;    ssl\\\\\\\_certificate\\\\\\\_key /etc/letsencrypt/live/domeniu-tau.ro/privkey.pem;    location / {        proxy\\\\\\\_pass http://127.0.0.1:3000;        proxy\\\\\\\_set\\\\\\\_header Host $host;    }}``````bashsudo certbot --nginx -d domeniu-tau.ro -d www.domeniu-tau.ro -d grafana.domeniu-tau.rosudo nginx -t \\\\\\\&\\\\\\\& sudo systemctl reload nginx```\---## Faza 6 — GitHub și documentație (Pe tot parcursul)### Structura repository```devops-homelab/├── README.md                    ← documentație principală cu poze├── .gitignore├── .github/│   └── workflows/│       ├── ci.yml│       └── deploy.yml├── app/                         ← codul aplicației│   ├── Dockerfile│   ├── docker-compose.yml│   ├── requirements.txt│   └── ...├── infrastructure/│   ├── nginx/│   │   └── nginx.conf│   ├── monitoring/│   │   ├── prometheus.yml│   │   ├── loki.yml│   │   ├── promtail.yml│   │   └── grafana/│   └── wazuh/│       └── ossec.conf├── scripts/│   ├── setup.sh                 ← script instalare completă│   ├── backup.sh│   └── health-check.sh└── docs/    ├── architecture.md    ├── setup-guide.md    ├── security-checklist.md    └── screenshots/             ← poze cu Grafana, Wazuh etc.```### README.md de nota 10```markdown# DevSecOps Home Lab> Pipeline CI/CD complet cu security scanning, monitoring și SIEM> pe VPS Oracle Cloud Free Tier## Arhitectură\\\\\\\[diagramă]## Features- CI/CD automat cu GitHub Actions la fiecare commit- SAST cu Semgrep (OWASP Top 10)- Container scanning cu Trivy- Secret detection cu GitLeaks- Monitoring cu Prometheus + Grafana- Log aggregation cu Loki- SIEM/IDS cu Wazuh- SSL automat cu Let's Encrypt- Rate limiting și security headers Nginx## Live Demo- Aplicație: https://domeniu-tau.ro- Grafana: https://grafana.domeniu-tau.ro## Pipeline Status!\\\\\\\[CI](https://github.com/bogdanbontos/devops-homelab/actions/workflows/ci.yml/badge.svg)## Setup\\\\\\\[ghid detaliat]## Tehnologii folosite\\\\\\\[tabel cu toate tehnologiile]```\---## Faza 7 — Îmbunătățiri avansate (Opționale)### 7.1 DAST cu OWASP ZAP```yaml# Adaugă în ci.yml după deploy  dast:    name: DAST Scan (OWASP ZAP)    needs: deploy    runs-on: ubuntu-latest        steps:      - name: ZAP Full Scan        uses: zaproxy/action-full-scan@v0.7.0        with:          target: 'https://domeniu-tau.ro'          fail\\\\\\\_action: false          cmd\\\\\\\_options: '-a'```### 7.2 Kubernetes cu k3sCând te simți confortabil cu Docker, următorul pas:```bash# Instalare k3s (Kubernetes lightweight)curl -sfL https://get.k3s.io | sh -# Verificarekubectl get nodeskubectl get pods -A```### 7.3 HashiCorp Vault pentru secretsNu mai stochezi parole în .env — le gestionezi centralizat:```bash# Instalare Vault în Dockerdocker run -d \\\\\\\\    --name vault \\\\\\\\    -p 127.0.0.1:8200:8200 \\\\\\\\    -e 'VAULT\\\\\\\_DEV\\\\\\\_ROOT\\\\\\\_TOKEN\\\\\\\_ID=token-dev' \\\\\\\\    vault:latest# Stocare secretvault kv put secret/myapp db\\\\\\\_pass=parola\\\\\\\_mea\\\\\\\_sigura```### 7.4 Alerting pe Telegram```yaml# În prometheus - alertmanager.ymlroute:  receiver: 'telegram'receivers:  - name: 'telegram'    telegram\\\\\\\_configs:      - bot\\\\\\\_token: 'TOKEN'        chat\\\\\\\_id: CHAT\\\\\\\_ID        message: '{{ range .Alerts }}{{ .Annotations.summary }}{{ end }}'```\---## Timeline recomandat|Săptămână|Ce faci|Rezultat||-|-|-||1-2|Aplicația + Docker|Container funcțional local||2-3|VPS securizat|Server hardened, SSH ok||3-4|GitHub Actions CI/CD|Deploy automat la fiecare push||4-5|Prometheus + Grafana + Loki|Dashboard monitoring live||5-6|Wazuh SIEM|Detecție intruziuni activă||6|Nginx + SSL + security headers|Site live cu HTTPS||7+|Îmbunătățiri: ZAP, k3s, Vault|Nivel avansat|**Total estimat: 6-8 săptămâni pentru versiunea completă**\---## Ce scrii pe CV după```DevSecOps Home Lab Project (2026)• Implementat pipeline CI/CD complet cu GitHub Actions incluzând  SAST (Semgrep), container scanning (Trivy) și secret detection (GitLeaks)• Configurat stack de monitoring Prometheus/Grafana/Loki cu alerting automat• Implementat SIEM open-source (Wazuh) pentru detecție intruziuni și FIM• Securizat infrastructură cu Nginx (security headers, rate limiting),  UFW, Fail2ban și SSH hardening• Toate componentele containerizate cu Docker și orchestrate cu Docker ComposeTehnologii: Docker, GitHub Actions, Prometheus, Grafana, Loki, Wazuh,Nginx, Python Flask, Linux, UFW, Fail2banLink: github.com/bogdanbontos/devops-homelab```\---## Resurse pentru fiecare componentă|Componentă|Resursă||-|-||GitHub Actions|docs.github.com/actions||Semgrep|semgrep.dev/docs||Trivy|aquasecurity.github.io/trivy||Prometheus|prometheus.io/docs||Grafana|grafana.com/docs||Loki|grafana.com/docs/loki||Wazuh|documentation.wazuh.com||Docker|docs.docker.com||Nginx|nginx.org/en/docs|\---*Document generat: Sâmbătă, 21 Martie 2026Autor: Claude pentru Bogdan Mihai BontoșVersiune: 1.0 — Plan DevSecOps Home Lab*